# Changelog 1.1.3

🔧 Fixes:
- Fix bind `bound_update` to `UpdateCute` and incoming update
- Add parameter `bound_api` for `from_raw`, `from_mapping`, `from_dict`, `from_data` methods of `BaseCute` to correct bind `bound_api`

🧰 Dependeices:
- Bump `vbml2` from `2.0.0` to `2.0.1`
